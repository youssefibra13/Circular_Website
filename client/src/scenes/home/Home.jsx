import MainCarousel from "./MainCarousel";

// Creating component Home to initialize Homepage
const Home = () => {
    return (
        <div className = "home">
            <MainCarousel />
        </div>
    );
};

export default Home;